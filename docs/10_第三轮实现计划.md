# 第三轮实现计划（v1.1.7：迭代+审计修复+评价改进）

生成日期：2026-09-20
关联：docs/08 第二轮迭代规划、docs/09 第二轮代码审计报告

> 用户确认：把「迭代 / 审计 / 评价」三块作为一次发布合并修改，版本 1.1.6 → 1.1.7。

## 一、范围与步骤

| # | 事项 | 位置 | 风险 |
| --- | --- | --- | --- |
| S1 | 版本号 1.1.7 | package.json / Cargo.toml / tauri.conf.json | 低 |
| S2 | 评价触发放宽：评分与赞助解耦、可重复提醒、深链失败回退+日志 | useAppLogic.ts / RateModal.tsx | 低 |
| S3 | 审计 P1：AI 导出 HTML 转义（防 XSS） | AIInsightsView.tsx handleExport | 低 |
| S4 | 审计 P2：tauri.conf.json 增加 CSP | tauri.conf.json | 中(需运行验证) |
| S5 | 审计 P3：search 正则长度上限；AIInsights 3 处硬编码错误串转 t() | search.rs / AIInsightsView.tsx | 低 |
| S6 | 审计 P2：find_duplicates 加取消令牌（后端） | duplicates.rs / state.rs | 中 |
| S7 | 回归验证 + 提交 | cargo check / tsc / i18n 脚本 | — |

## 二、S2 评价触发（核心，来自讨论结论）
现状问题：赞助弹窗永远优先、评分每次启动至多弹一次、且要在「赞助+评分 key 同时 true」才重置 → 评分提示频次极低 → 零评价。
改法：
- 将 sponsor 与 rate 拆成**各自独立的节流**（各自有 first-run 延迟与 hasShown 标记，互不卡顿）。
- 评分在「首次运行满 N 天且本会话未询问」时展示；展示后**不永久隐藏**，允许在每个新启动、间隔足够长（如 ≥ 若干天）后再次提醒（把「一次性 hasShown」改为「记录最近提示时间」节流）。
- 深链失败：RateModal 先深链 `ms-windows-store://review/?productid=...`，catch 后回退打开商店详情页 `webUrl`，仍失败回退 `web`；catch 分支打 `console.warn` 便于排查。
- 全程保持 `isTauri()`/`isWindows()` 门控与 `t()` 文案。

## 三、S3 审计 P1（优先）
handleExport 用 `\`\`` 模板拼 `i.reason`/`i.action` 与路径生成 `.html`。加入 HTML 转义函数（`& < > " '` → 实体），应用于所有来自 LLM/路径的插值，再拼接示例/配色等固定样式。

## 四、S4 CSP（保守）
tauri.conf.json `app.security` 增内容安全策略，放开必要源：
`default-src 'self'; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost; script-src 'self'`
（若构建后页面异常需回退放宽；本机无法运行冒烟，列为待测试项并在提交说明标注。）

## 五、S5 P3 小项
- search.rs：用户 regex 长度上限（如 ≤512）后拒绝，避免异常长输入。
- AIInsightsView 残留 3 处硬编码英文错误串 → 补 t() key，改 en/zh base + `node update_locales.cjs && node translation_check.cjs`。

## 六、S6 审计 P2（find_duplicates 取消，后端）
- state.rs 增加 `duplicate_scan_cancel_token: Arc<AtomicBool>`（`Default`）。
- duplicates.rs 在命令开头存入新 token，find_duplicates 的 walk/hash 各阶段检查该 token，命中即返回错误/中断。
- 新增 `cancel_find_duplicates` 命令并在 lib.rs 注册；前端按钮（可选，若时间/风险可接受）后续接。
- 说明：自身遍历用 rayon par_iter，取消只能做到「下一批前检查」，无法逐条中断——在计划中明确为尽力而为。

## 七、验证与提交
- `cd src-tauri && cargo check`；`npx tsc --noEmit`。
- i18n 后 `node update_locales.cjs && node translation_check.cjs`。
- 单个发布 commit 提交（版本 1.1.7，含三块）。